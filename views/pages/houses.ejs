<% include ../partials/head %>
<% include ../partials/header %>
<style>
	.cell {
		border: 1px solid lightgrey;
		border-radius: 5px;
		margin-bottom: 5px;
		padding: 5px;
	}
	.form-set {
		border-radius: 5px;
		padding: 5px;
		margin: 5px;
		display: none;
	}
	#contact-btn {
		margin: 10px;
		position: relative;
	}
</style>
<script>
	// $(document).ready(function() {
	// 	$('#contact-btn').on('click', function() {
	// 		$('.form-set').toggle();
	// 	});
	// });

</script>
<!-- <div class="container">
	<button id="contact-btn" class="btn btn-primary pull-right">Add New Contact</button>
</div> -->
<div class="container">
	<div class="container form-set">
		<form class="form-horizontal" method="post" action="/houses">
		  <div class="form-group">
		    <label for="name" class="col-sm-2 control-label">Name</label>
		    <div class="col-sm-8">
		      <input type="text" class="form-control" name="name" id="name" placeholder="Name" value="<%= contact.name %>">
		    </div>
		  </div>
		  <div class="form-group">
		    <label for="Phone" class="col-sm-2 control-label">Phone Number</label>
		    <div class="col-sm-8">
		      <input type="phone" name="phone" class="form-control" id="phone" placeholder="Phone" value="<%= contact.phone_number %>">
		    </div>
		  </div>
		  <div class="form-group">
		    <label for="Email" class="col-sm-2 control-label">Email</label>
		    <div class="col-sm-8">
		      <input type="email" name="email" class="form-control" id="email" placeholder="Email" value="<%= contact.email %>">
		    </div>
		  </div>
		  <div class="form-group">
			<label for="House" class="col-sm-2 control-label">House</label>
			<div class="col-sm-8">
				  <select class="form-control" name="house">
					  <% for(var i=0; i < people.length; i++){ %>
						<option><%= people[i].name %></option>
					  <% } %>
					</select>
			</div>
		  </div>
		  <div class="form-group">
		    <div class="col-sm-offset-2 col-sm-8">
		      <button type="submit" class="btn btn-primary">Add To List</button>
		    </div>
		  </div>
		</form>

	</div>
</div>
<div class="container">
<% for(var i=0; i < people.length; i++) { %>
	<div class="cell">
		<h3 class=""><%= people[i].name %></h3>
		<hr>
		<h4 class=""><%= people[i].address %></h4>
		<% if(people[i].contact.length > 0) { %>
			<p>family.contact</p>
		<% } %>
	</div>
<% } %>
</div>


<% include ../partials/footer %>